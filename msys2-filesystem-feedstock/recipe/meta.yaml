# This file was generated by gen-conda-repack-recipe
# using the MSYS2 package epoch: Jul 01 2026 06:00:00 UTC
#
# Any edits will be overwritten
#
# https://github.com/msys2/MSYS2-packages/blob/master/filesystem/PKGBUILD
{% set pkgname = "filesystem" %}
{% set pkgver = "2026.03.06" %}
{% set pkgrel = "1" %}
{% set build_number = pkgrel|int * 1000 + 0 %}

package:
  name: repack_msys2-filesystem
  version: {{ pkgver }}

source:
  - url: https://repo.msys2.org/msys/x86_64/filesystem-{{ pkgver }}-{{ pkgrel }}-x86_64.pkg.tar.zst
    folder: msys2-filesystem
    sha256: 4506b2dbb496281b7645ee2b2d3700f6a2fab3d74a800733f3b3b44eeb8edc5c

build:
  number: {{ build_number }}
  binary_relocation: false
  detect_binary_files_with_prefix: false
  skip: True             # [not win]

outputs:
  - name: msys2-filesystem
    skip: True          # [not (win)]
    script: repack.bat  # [win]
    build:
      number: {{ build_number }}
    requirements:
      build:
      host:
      run:
        - msys2-msys-mutex * msys2
      run_constrained:
        - m2-filesystem                            <0a0
        - m2-msys2-runtime                         <0a0
    test:
      commands:
        - test -f $PREFIX/Library/usr/bin/cmd                                    # [unix]
        - IF NOT EXIST %PREFIX%\Library\usr\bin\cmd EXIT /B 1                    # [win]
    about:
      home: https://github.com/msys2/MSYS2-packages/tree/master/filesystem
      summary: "Base filesystem (repack of MSYS2-packages filesystem for MSYS)"
      license: BSD-3-Clause
      license_url:
        - https://spdx.org/licenses/BSD-3-Clause.html

