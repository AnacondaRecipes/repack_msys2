# This file was generated by msys2_build (Python)
# using the MSYS2 package epoch: Jul 01 2026 06:00:00 UTC
#
# Any edits will be overwritten
#
# https://github.com/msys2/MINGW-packages/blob/master/mingw-w64-libmangle/PKGBUILD
{% set pkgname = "libmangle" %}
{% set pkgver = "14.0.0.r147.g31bd54ab7" %}
{% set pkgrel = "1" %}
{% set build_number = pkgrel|int * 1000 + 0 %}

package:
  name: repack_mingw-w64-libmangle-git
  version: {{ pkgver }}

source:
  - url: https://repo.msys2.org/mingw/ucrt64/mingw-w64-ucrt-x86_64-libmangle-{{ pkgver }}-{{ pkgrel }}-any.pkg.tar.zst # [win and x86_64]
    folder: ucrt64-libmangle # [win and x86_64]
    sha256: 94f0e1f19ea32a83cbb617c9ee756ddaa3c297136f952bbb8da98c3c9a5234d0 # [win and x86_64]
  - url: https://repo.msys2.org/mingw/clangarm64/mingw-w64-clang-aarch64-libmangle-{{ pkgver }}-{{ pkgrel }}-any.pkg.tar.zst # [win and arm64]
    folder: clangarm64-libmangle # [win and arm64]
    sha256: 9267ccc9ac993be6f2ee66d31e6904c4e53be3d972b9f4a998851d3798a4a4bd # [win and arm64]

build:
  number: {{ build_number }}
  binary_relocation: false
  detect_binary_files_with_prefix: false
  skip: True             # [not win]

outputs:
  - name: ucrt64-libmangle
    skip: True          # [not (win and x86_64)]
    script: repack.bat  # [win]
    build:
      number: {{ build_number }}
    requirements:
      build:
      host:
      run:
        - msys2-mingw-w64-mutex * ucrt64
      run_constrained:
        - m2w64-libmangle                          <0a0
    test:
      commands:
        - test -f $PREFIX/Library/ucrt64/include/libmangle.h                     # [unix]
        - IF NOT EXIST %PREFIX%\Library\ucrt64\include\libmangle.h EXIT /B 1     # [win]
    about:
      home: https://www.mingw-w64.org/
      summary: "MinGW-w64 libmangle (mingw-w64) (repack of MINGW-packages libmangle for UCRT64)"
      license: MIT
      license_file:
        - {{ RECIPE_DIR }}/src_licenses/clangarm64/share/licenses/mingw-w64-libraries/COPYING.libmangle
        - {{ RECIPE_DIR }}/src_licenses/ucrt64/share/licenses/mingw-w64-libraries/COPYING.libmangle

  - name: clangarm64-libmangle
    skip: True          # [not (win and arm64)]
    script: repack.bat  # [win]
    build:
      number: {{ build_number }}
    requirements:
      build:
      host:
      run:
        - msys2-mingw-w64-mutex * clangarm64
    test:
      commands:
        - test -f $PREFIX/Library/clangarm64/include/libmangle.h                 # [unix]
        - IF NOT EXIST %PREFIX%\Library\clangarm64\include\libmangle.h EXIT /B 1 # [win]
    about:
      home: https://www.mingw-w64.org/
      summary: "MinGW-w64 libmangle (mingw-w64) (repack of MINGW-packages libmangle for CLANGARM64)"
      license: MIT
      license_file:
        - {{ RECIPE_DIR }}/src_licenses/clangarm64/share/licenses/mingw-w64-libraries/COPYING.libmangle
        - {{ RECIPE_DIR }}/src_licenses/ucrt64/share/licenses/mingw-w64-libraries/COPYING.libmangle

