# This file was generated by msys2_build (Python)
# using the MSYS2 package epoch: Jul 01 2026 06:00:00 UTC
#
# Any edits will be overwritten
#
{% set pkgname = "repack_msys2-perl-net-http" %}
{% set pkgver = "6.24" %}
{% set pkgrel = "1" %}
{% set build_number = pkgrel|int * 1000 + 0 %}

package:
  name: {{ pkgname }}
  version: {{ pkgver }}

source:
  - url: https://repo.msys2.org/msys/x86_64/perl-Net-HTTP-{{ pkgver }}-{{ pkgrel }}-any.pkg.tar.zst
    folder: msys2-perl-net-http
    sha256: 2bdd43d95f1f7be7d9301bd82a40e149893c272c3df6cee7a197d658a046d931

build:
  number: {{ build_number }}
  binary_relocation: false
  detect_binary_files_with_prefix: false
  skip: True             # [not win]

outputs:
  - name: msys2-perl-net-http
    skip: True          # [not (win)]
    script: repack.bat  # [win]
    build:
      number: 1000
    requirements:
      run:
        - msys2-msys-mutex * msys2
        - msys2-perl >=5.6.2
      run_constrained:
        - m2-perl-Net-HTTP                                   <0a0
    test:
      commands:
        - test -f $PREFIX/Library/usr/share/man/man3/Net.HTTP.3pm.gz             # [unix]
        - IF NOT EXIST %PREFIX%\Library\usr\share\man\man3\Net.HTTP.3pm.gz EXIT /B 1 # [win]
    about:
      home: https://metacpan.org/release/Net-HTTP
      summary: "Low-level HTTP connection (client) (repack of MSYS2-packages perl-Net-HTTP for MSYS)"
      license: spdx:Artistic-1.0-Perl OR GPL-1.0-or-later

