# This file was generated by gen-conda-repack-recipe
# using the MSYS2 package epoch: Jul 28 2026 14:00:00 UTC
#
# Any edits will be overwritten
#
# https://github.com/msys2/MSYS2-packages/blob/master/bash-completion/PKGBUILD
{% set pkgname = "bash-completion" %}
{% set pkgver = "2.18.0" %}
{% set pkgrel = "1" %}
{% set build_number = pkgrel|int * 1000 + 0 %}

package:
  name: repack_msys2-bash-completion
  version: {{ pkgver }}

source:
  - url: https://repo.msys2.org/msys/x86_64/bash-completion-{{ pkgver }}-{{ pkgrel }}-any.pkg.tar.zst
    folder: msys2-bash-completion
    sha256: 1babcc1d94e5e0d4e96d18c532ea2c5a6841dc64709cbe8365385511df5e932d

build:
  number: {{ build_number }}
  binary_relocation: false
  detect_binary_files_with_prefix: false
  skip: True             # [not win]

outputs:
  - name: msys2-bash-completion
    skip: True          # [not (win)]
    script: repack.bat  # [win]
    build:
      number: {{ build_number }}
    requirements:
      build:
      host:
      run:
        - msys2-msys-mutex * msys2
        - msys2-bash                               # bash
      run_constrained:
        - m2-bash-completion                       <0a0
        - m2-msys2-runtime                         <0a0
    test:
      commands:
        - IF NOT EXIST %PREFIX%\Library\usr\share\bash-completion\bash_completion EXIT /B 1 # [win]
    about:
      home: https://github.com/scop/bash-completion
      summary: "Programmable completion for the bash shell (repack of MSYS2-packages bash-completion for MSYS)"
      license: GPL-2.0-only
      license_file:
        - {{ RECIPE_DIR }}/src_licenses/COPYING

