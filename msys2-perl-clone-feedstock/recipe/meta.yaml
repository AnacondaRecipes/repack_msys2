# This file was generated by msys2_build (Python)
# using the MSYS2 package epoch: Jul 01 2026 06:00:00 UTC
#
# Any edits will be overwritten
#
{% set pkgname = "repack_msys2-perl-clone" %}
{% set pkgver = "0.50" %}
{% set pkgrel = "2" %}
{% set build_number = pkgrel|int * 1000 + 0 %}

package:
  name: {{ pkgname }}
  version: {{ pkgver }}

source:
  - url: https://repo.msys2.org/msys/x86_64/perl-Clone-{{ pkgver }}-{{ pkgrel }}-x86_64.pkg.tar.zst
    folder: msys2-perl-clone
    sha256: fd131c4b6c12d29525a3f14382f001ba1bdb2e65be3f55afe9c82d7fb29e2224

build:
  number: {{ build_number }}
  binary_relocation: false
  detect_binary_files_with_prefix: false
  skip: True             # [not win]

outputs:
  - name: msys2-perl-clone
    skip: True          # [not (win)]
    script: repack.bat  # [win]
    build:
      number: {{ build_number }}
      missing_dso_whitelist:
        - $RPATH/KERNEL32.dll                                  # [win]
    requirements:
      run:
        - msys2-msys-mutex * msys2
        - msys2-perl >=5.42.2
      run_constrained:
        - m2-perl-clone                            <0a0
    test:
      commands:
        - test -f $PREFIX/Library/usr/lib/perl5/vendor_perl/auto/Clone/Clone.dll # [unix]
        - IF NOT EXIST %PREFIX%\Library\usr\lib\perl5\vendor_perl\auto\Clone\Clone.dll EXIT /B 1 # [win]
    about:
      home: https://github.com/garu/Clone
      summary: "Recursive copy of nested objects. (repack of MSYS2-packages perl-Clone for MSYS)"
      license: GPL-1.0-or-later OR Artistic-1.0-Perl
      license_url:
        - https://dev.perl.org/licenses/

