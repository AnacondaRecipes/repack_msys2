# This file was generated by gen-conda-repack-recipe
# using the MSYS2 package epoch: Jul 28 2026 14:00:00 UTC
#
# Any edits will be overwritten
#
# https://github.com/msys2/MSYS2-packages/blob/master/nano/PKGBUILD
{% set pkgname = "nano" %}
{% set pkgver = "9.1" %}
{% set pkgrel = "1" %}
{% set build_number = pkgrel|int * 1000 + 0 %}

package:
  name: repack_msys2-nano
  version: {{ pkgver }}

source:
  - url: https://repo.msys2.org/msys/x86_64/nano-{{ pkgver }}-{{ pkgrel }}-x86_64.pkg.tar.zst
    folder: msys2-nano
    sha256: 4bb57016ec623e1d3ee212da82d061e08778d6058a7b6bb1444df986637f4b98

build:
  number: {{ build_number }}
  binary_relocation: false
  detect_binary_files_with_prefix: false
  skip: True             # [not win]

outputs:
  - name: msys2-nano
    skip: True          # [not (win)]
    script: repack.bat  # [win]
    build:
      number: {{ build_number }}
      missing_dso_whitelist:
        - $RPATH/KERNEL32.dll                                # [win]
        - $RPATH/msys-2.0.dll                                # [win]
        - $RPATH/msys-intl-8.dll                             # [win]
        - $RPATH/msys-magic-1.dll                            # [win]
        - $RPATH/msys-ncursesw6.dll                          # [win]
    requirements:
      build:
      host:
      run:
        - msys2-msys-mutex * msys2
        - msys2-file                               # file
        - msys2-libintl                            # libintl
        - msys2-ncurses                            # ncurses
        - msys2-bash                               # sh
        - msys2-msys2-runtime                      # DSO msys-2.0.dll
      run_constrained:
        - m2-nano                                  <0a0
        - m2-msys2-runtime                         <0a0
    test:
      commands:
        - IF NOT EXIST %PREFIX%\Library\usr\bin\nano.exe EXIT /B 1               # [win]
        - nano --help                                                            # [win]
        - rnano --help                                                           # [win]
    about:
      home: https://www.nano-editor.org
      summary: "Pico editor clone with enhancements (repack of MSYS2-packages nano for MSYS)"
      license: GPL-3.0-or-later and GFDL-1.2
      license_file:
        - {{ RECIPE_DIR }}/src_licenses/COPYING
        - {{ RECIPE_DIR }}/src_licenses/COPYING.DOC

