# This file was generated by msys2_build (Python)
# using the MSYS2 package epoch: Jul 01 2026 06:00:00 UTC
#
# Any edits will be overwritten
#
{% set pkgname = "repack_msys2-perl-http-date" %}
{% set pkgver = "6.07" %}
{% set pkgrel = "1" %}
{% set build_number = pkgrel|int * 1000 + 0 %}

package:
  name: {{ pkgname }}
  version: {{ pkgver }}

source:
  - url: https://repo.msys2.org/msys/x86_64/perl-HTTP-Date-{{ pkgver }}-{{ pkgrel }}-any.pkg.tar.zst
    folder: msys2-perl-http-date
    sha256: 143fcd3a1d4cd685ac9b617259ef8b3cdb731bb29b19e01ac3c16883765d493b

build:
  number: {{ build_number }}
  binary_relocation: false
  detect_binary_files_with_prefix: false
  skip: True             # [not win]

outputs:
  - name: msys2-perl-http-date
    skip: True          # [not (win)]
    script: repack.bat  # [win]
    build:
      number: 1000
    requirements:
      run:
        - msys2-msys-mutex * msys2
        - msys2-perl >=5.8.8
      run_constrained:
        - m2-perl-HTTP-Date                                   <0a0
    test:
      commands:
        - test -f $PREFIX/Library/usr/share/man/man3/HTTP.Date.3pm.gz            # [unix]
        - IF NOT EXIST %PREFIX%\Library\usr\share\man\man3\HTTP.Date.3pm.gz EXIT /B 1 # [win]
    about:
      home: https://metacpan.org/release/HTTP-Date
      summary: "Date conversion routines (repack of MSYS2-packages perl-HTTP-Date for MSYS)"
      license: spdx:Artistic-1.0-Perl OR GPL-1.0-or-later

