# This file was generated by gen-conda-repack-recipe
# using the MSYS2 package epoch: Sep 01 2024 06:00:00 UTC
#
# Any edits will be overwritten
#
# https://github.com/msys2/MSYS2-packages/blob/master/bison/PKGBUILD
{% set pkgname = "bison" %}
{% set pkgver = "3.8.2" %}
{% set pkgrel = "5" %}
{% set build_number = pkgrel|int * 1000 + 0 %}

package:
  name: repack_msys2-bison
  version: {{ pkgver }}

source:
  - url: https://repo.msys2.org/msys/x86_64/bison-{{ pkgver }}-{{ pkgrel }}-x86_64.pkg.tar.zst
    folder: msys2-bison
    sha256: 7439ca784a9909e0730a513d3aff15852c3b292ac85053fa2dd7a2a19c1ce04c

build:
  number: {{ build_number }}
  binary_relocation: false
  detect_binary_files_with_prefix: false
  skip: True             # [not win]

outputs:
  - name: msys2-bison
    skip: True          # [not (win and x86_64)]
    script: repack.bat  # [win]
    build:
      number: {{ build_number }}
      missing_dso_whitelist:
        - $RPATH/msys-2.0.dll                                # [win]
        - $RPATH/msys-iconv-2.dll                            # [win]
        - $RPATH/msys-intl-8.dll                             # [win]
    requirements:
      build:
      host:
      run:
        - msys2-msys-mutex * msys2
        - msys2-m4                                 >=1.4.19     # m4
        - msys2-bash                               >=5.2.032    # PKGBUILD sh provided by bash
        - msys2-libiconv                           >=1.17       # libiconv
        - msys2-libintl                            >=0.22.4     # libintl
        - msys2-msys2-runtime                      >=3.5.4      # msys-2.0.dll
      run_constrained:
        - m2-bison                                  <0a0
        - m2-msys2-runtime                          <0a0

    test:
      commands:
        - test -f $PREFIX/Library/usr/bin/bison.exe                              # [unix]
        - IF NOT EXIST %PREFIX%\Library\usr\bin\bison.exe EXIT /B 1              # [win]
        - bison --help                                                           # [win and x86_64]
    about:
      home: https://www.gnu.org/software/bison/bison.html
      summary: "The GNU general-purpose parser generator (repack of MSYS2-packages bison for MSYS)"
      license: GPL-3.0-or-later
      license_file:
        - {{ RECIPE_DIR }}/src_licenses/usr/share/doc/bison/COPYING
