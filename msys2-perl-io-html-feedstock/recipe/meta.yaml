# This file was generated by msys2_build (Python)
# using the MSYS2 package epoch: Jul 01 2026 06:00:00 UTC
#
# Any edits will be overwritten
#
{% set pkgname = "repack_msys2-perl-io-html" %}
{% set pkgver = "1.004" %}
{% set pkgrel = "2" %}
{% set build_number = pkgrel|int * 1000 + 0 %}

package:
  name: {{ pkgname }}
  version: {{ pkgver }}

source:
  - url: https://repo.msys2.org/msys/x86_64/perl-IO-HTML-{{ pkgver }}-{{ pkgrel }}-any.pkg.tar.zst
    folder: msys2-perl-io-html
    sha256: d84e9eb722f7e256cfbc8d84894e7413b68498fbda08df83f75a08807bafa3e1

build:
  number: {{ build_number }}
  binary_relocation: false
  detect_binary_files_with_prefix: false
  skip: True             # [not win]

outputs:
  - name: msys2-perl-io-html
    skip: True          # [not (win)]
    script: repack.bat  # [win]
    build:
      number: {{ build_number }}
    requirements:
      run:
        - msys2-msys-mutex * msys2
        - msys2-perl >=5.42.2
      run_constrained:
        - m2-perl-io-html                          <0a0
    test:
      commands:
        - test -f $PREFIX/Library/usr/share/man/man3/IO.HTML.3pm.gz              # [unix]
        - IF NOT EXIST %PREFIX%\Library\usr\share\man\man3\IO.HTML.3pm.gz EXIT /B 1 # [win]
    about:
      home: https://metacpan.org/release/IO-HTML
      summary: "Open an HTML file with automatic charset detection (repack of MSYS2-packages perl-IO-HTML for MSYS)"
      license: GPL-1.0-or-later OR Artistic-1.0-Perl
      license_file:
        - {{ RECIPE_DIR }}/src_licenses/LICENSE

