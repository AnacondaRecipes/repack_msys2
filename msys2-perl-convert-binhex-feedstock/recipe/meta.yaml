# This file was generated by msys2_build (Python)
# using the MSYS2 package epoch: Jul 01 2026 06:00:00 UTC
#
# Any edits will be overwritten
#
{% set pkgname = "repack_msys2-perl-convert-binhex" %}
{% set pkgver = "1.125" %}
{% set pkgrel = "2" %}
{% set build_number = pkgrel|int * 1000 + 0 %}

package:
  name: {{ pkgname }}
  version: {{ pkgver }}

source:
  - url: https://repo.msys2.org/msys/x86_64/perl-Convert-BinHex-{{ pkgver }}-{{ pkgrel }}-any.pkg.tar.zst
    folder: msys2-perl-convert-binhex
    sha256: 164e802322370041dbb78d07ad3b59e017de1d281794734889bc905c273b4923

build:
  number: {{ build_number }}
  binary_relocation: false
  detect_binary_files_with_prefix: false
  skip: True             # [not win]

outputs:
  - name: msys2-perl-convert-binhex
    skip: True          # [not (win)]
    script: repack.bat  # [win]
    build:
      number: 2000
    requirements:
      run:
        - msys2-msys-mutex * msys2
        - msys2-perl >=5.42.2
      run_constrained:
        - m2-perl-Convert-BinHex                                   <0a0
    test:
      commands:
        - test -f $PREFIX/Library/usr/bin/vendor_perl/binhex.pl                  # [unix]
        - IF NOT EXIST %PREFIX%\Library\usr\bin\vendor_perl\binhex.pl EXIT /B 1  # [win]
    about:
      home: https://metacpan.org/dist/Convert-BinHex
      summary: "Perl module to extract data from Macintosh BinHex files (repack of MSYS2-packages perl-Convert-BinHex for MSYS)"
      license: spdx:Artistic-1.0-Perl OR GPL-1.0-or-later

