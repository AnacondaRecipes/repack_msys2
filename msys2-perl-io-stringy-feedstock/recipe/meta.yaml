# This file was generated by msys2_build (Python)
# using the MSYS2 package epoch: Jul 01 2026 06:00:00 UTC
#
# Any edits will be overwritten
#
# https://github.com/msys2/MSYS2-packages/blob/master/perl-io-stringy/PKGBUILD
{% set pkgname = "perl-io-stringy" %}
{% set pkgver = "2.113" %}
{% set pkgrel = "3" %}
{% set build_number = pkgrel|int * 1000 + 0 %}

package:
  name: repack_msys2-perl-io-stringy
  version: {{ pkgver }}

source:
  - url: https://repo.msys2.org/msys/x86_64/perl-IO-Stringy-{{ pkgver }}-{{ pkgrel }}-any.pkg.tar.zst
    folder: msys2-perl-io-stringy
    sha256: eda6107fa2f077f899e92cf84822751439f9152ea98bea2f25bd657a90ee21eb

build:
  number: {{ build_number }}
  binary_relocation: false
  detect_binary_files_with_prefix: false
  skip: True             # [not win]

outputs:
  - name: msys2-perl-io-stringy
    skip: True          # [not (win)]
    script: repack.bat  # [win]
    build:
      number: {{ build_number }}
    requirements:
      build:
      host:
      run:
        - msys2-msys-mutex * msys2
        - msys2-perl >=5.42.2                      # perl
      run_constrained:
        - m2-perl-io-stringy                       <0a0
        - m2-msys2-runtime                         <0a0
    test:
      commands:
        - test -f $PREFIX/Library/usr/share/man/man3/IO.AtomicFile.3pm.gz        # [unix]
        - IF NOT EXIST %PREFIX%\Library\usr\share\man\man3\IO.AtomicFile.3pm.gz EXIT /B 1 # [win]
    about:
      home: https://metacpan.org/dist/IO-Stringy
      summary: "I/O on in-core objects like strings and arrays (repack of MSYS2-packages perl-IO-Stringy for MSYS)"
      license: GPL-1.0-or-later OR Artistic-1.0-Perl
      license_file:
        - {{ RECIPE_DIR }}/src_licenses/LICENSE

