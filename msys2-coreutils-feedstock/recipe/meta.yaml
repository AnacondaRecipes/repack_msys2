# This file was generated by gen-conda-repack-recipe
# using the MSYS2 package epoch: Jul 01 2026 06:00:00 UTC
#
# Any edits will be overwritten
#
# https://github.com/msys2/MSYS2-packages/blob/master/coreutils/PKGBUILD
{% set pkgname = "coreutils" %}
{% set pkgver = "8.32" %}
{% set pkgrel = "5" %}
{% set build_number = pkgrel|int * 1000 + 0 %}

package:
  name: repack_msys2-coreutils
  version: {{ pkgver }}

source:
  - url: https://repo.msys2.org/msys/x86_64/coreutils-{{ pkgver }}-{{ pkgrel }}-x86_64.pkg.tar.zst
    folder: msys2-coreutils
    sha256: 62dfee1c39fd15f99c39802b35e82643bc14fffc16d6c76d4001caa385ec77e3

build:
  number: {{ build_number }}
  binary_relocation: false
  detect_binary_files_with_prefix: false
  skip: True             # [not win]

outputs:
  - name: msys2-coreutils
    skip: True          # [not (win)]
    script: repack.bat  # [win]
    build:
      number: {{ build_number }}
      missing_dso_whitelist:
        - $RPATH/KERNEL32.dll                                # [win]
        - $RPATH/msys-2.0.dll                                # [win]
        - $RPATH/msys-gcc_s-seh-1.dll                        # [win]
        - $RPATH/msys-gmp-10.dll                             # [win]
        - $RPATH/msys-iconv-2.dll                            # [win]
        - $RPATH/msys-intl-8.dll                             # [win]
    requirements:
      build:
      host:
      run:
        - msys2-msys-mutex * msys2
        - msys2-gmp >=6.3.0                        # gmp
        - msys2-libiconv >=1.19                    # libiconv
        - msys2-libintl >=0.22.5                   # libintl
        - msys2-msys2-runtime >=3.6.9              # DSO msys-2.0.dll
        - msys2-gcc-libs >=15.3.0                  # DSO msys-gcc_s-seh-1.dll
      run_constrained:
        - m2-coreutils                             <0a0
        - m2-msys2-runtime                         <0a0
    test:
      commands:
        - "test -f $PREFIX/Library/usr/bin/[.exe"                                # [unix]
        - "IF NOT EXIST %PREFIX%\\Library\\usr\\bin\\[.exe EXIT /B 1"            # [win]
        - set "PATH=%PREFIX%\Library\usr\bin;%PATH%" && arch --help && b2sum --help && base32 --help && base64 --help && basename --help && basenc --help && cat --help && chcon --help # [win]
        - set "PATH=%PREFIX%\Library\usr\bin;%PATH%" && chgrp --help && chmod --help && chown --help && chroot --help && cksum --help && comm --help && cp --help && csplit --help # [win]
        - set "PATH=%PREFIX%\Library\usr\bin;%PATH%" && cut --help && dd --help && df --help && dircolors --help && dirname --help && du --help && echo --help && env --help # [win]
        - set "PATH=%PREFIX%\Library\usr\bin;%PATH%" && expand --help && expr --help && factor --help && fmt --help && fold --help && gkill --help && groups --help && head --help # [win]
        - set "PATH=%PREFIX%\Library\usr\bin;%PATH%" && hostid --help && hostname --help && id --help && install --help && link --help && ln --help && logname --help && ls --help # [win]
        - set "PATH=%PREFIX%\Library\usr\bin;%PATH%" && md5sum --help && mkdir --help && mkfifo --help && mknod --help && mktemp --help && mv --help && nice --help && nl --help # [win]
        - set "PATH=%PREFIX%\Library\usr\bin;%PATH%" && nohup --help && nproc --help && numfmt --help && od --help && paste --help && pathchk --help && pinky --help && pr --help # [win]
        - set "PATH=%PREFIX%\Library\usr\bin;%PATH%" && printenv --help && printf --help && ptx --help && pwd --help && readlink --help && realpath --help && rm --help && rmdir --help # [win]
        - set "PATH=%PREFIX%\Library\usr\bin;%PATH%" && runcon --help && seq --help && sha1sum --help && sha224sum --help && sha256sum --help && sha384sum --help && sha512sum --help && shred --help # [win]
        - set "PATH=%PREFIX%\Library\usr\bin;%PATH%" && shuf --help && sleep --help && sort --help && split --help && stat --help && stdbuf --help && stty --help && sum --help # [win]
        - set "PATH=%PREFIX%\Library\usr\bin;%PATH%" && sync --help && tac --help && tail --help && tee --help && test --help && timeout --help && touch --help && tr --help # [win]
        - set "PATH=%PREFIX%\Library\usr\bin;%PATH%" && true --help && truncate --help && tsort --help && tty --help && uname --help && unexpand --help && uniq --help && unlink --help # [win]
        - set "PATH=%PREFIX%\Library\usr\bin;%PATH%" && users --help && vdir --help && wc --help && who --help && whoami --help && yes --help # [win]
    about:
      home: https://www.gnu.org/software/coreutils
      summary: "The basic file, shell and text manipulation utilities of the GNU operating system (repack of MSYS2-packages coreutils for MSYS)"
      license: GPL-3.0-or-later
      license_file:
        - {{ RECIPE_DIR }}/src_licenses/COPYING

