# This file was generated by gen-conda-repack-recipe
# using the MSYS2 package epoch: Jul 01 2026 06:00:00 UTC
#
# Any edits will be overwritten
#
# https://github.com/msys2/MSYS2-packages/blob/master/util-linux/PKGBUILD
{% set pkgname = "util-linux" %}
{% set pkgver = "2.40.2" %}
{% set pkgrel = "2" %}
{% set build_number = pkgrel|int * 1000 + 0 %}

package:
  name: repack_msys2-util-linux
  version: {{ pkgver }}

source:
  - url: https://repo.msys2.org/msys/x86_64/util-linux-{{ pkgver }}-{{ pkgrel }}-x86_64.pkg.tar.zst
    folder: msys2-util-linux
    sha256: 969edb185d37986e6feddb3c54807598e3cc6774b736fec3a27d5d27731f7fdd
  - url: https://repo.msys2.org/msys/x86_64/libutil-linux-{{ pkgver }}-{{ pkgrel }}-x86_64.pkg.tar.zst
    folder: msys2-libutil-linux
    sha256: 148ec8e668cb0a30ea0c4e2687e16e0953d6628305598f4c3225bedf2564192d
  - url: https://repo.msys2.org/msys/x86_64/libutil-linux-devel-{{ pkgver }}-{{ pkgrel }}-x86_64.pkg.tar.zst
    folder: msys2-libutil-linux-devel
    sha256: 350434487de7d4cdd3c24afa75ac0d9a83d505b7247d1ea5172d9fffa5cd9efb

build:
  number: {{ build_number }}
  binary_relocation: false
  detect_binary_files_with_prefix: false
  skip: True             # [not win]

outputs:
  - name: msys2-util-linux
    skip: True          # [not (win)]
    script: repack.bat  # [win]
    build:
      number: {{ build_number }}
      missing_dso_whitelist:
        - $RPATH/KERNEL32.dll                                # [win]
        - $RPATH/msys-2.0.dll                                # [win]
        - $RPATH/msys-blkid-1.dll                            # [win]
        - $RPATH/msys-fdisk-1.dll                            # [win]
        - $RPATH/msys-intl-8.dll                             # [win]
        - $RPATH/msys-lastlog2-2.dll                         # [win]
        - $RPATH/msys-magic-1.dll                            # [win]
        - $RPATH/msys-ncursesw6.dll                          # [win]
        - $RPATH/msys-readline8.dll                          # [win]
        - $RPATH/msys-smartcols-1.dll                        # [win]
        - $RPATH/msys-uuid-1.dll                             # [win]
        - $RPATH/msys-z.dll                                  # [win]
    requirements:
      build:
      host:
      run:
        - msys2-msys-mutex * msys2
        - msys2-coreutils >=8.32                   # coreutils
        - msys2-libutil-linux >=2.40.2             # libutil-linux
        - msys2-libiconv >=1.19                    # libiconv
        - msys2-libreadline >=8.3.003              # libreadline
        - msys2-libsqlite >=3.53.3                 # libsqlite
        - msys2-msys2-runtime >=3.6.9              # DSO msys-2.0.dll
        - msys2-libintl >=0.22.5                   # DSO msys-intl-8.dll
        - msys2-file >=5.48                        # DSO msys-magic-1.dll
        - msys2-ncurses >=6.6                      # DSO msys-ncursesw6.dll
        - msys2-zlib >=1.3.2                       # DSO msys-z.dll
      run_constrained:
        - m2-util-linux                            <0a0
        - m2-msys2-runtime                         <0a0
    test:
      commands:
        - test -f $PREFIX/Library/usr/bin/agetty.exe                             # [unix]
        - IF NOT EXIST %PREFIX%\Library\usr\bin\agetty.exe EXIT /B 1             # [win]
        - set "PATH=%PREFIX%\Library\usr\bin;%PATH%" && agetty --help && blkid --help && cal --help && cfdisk --help && chrt --help && col --help && colcrt --help && colrm --help # [win]
        - set "PATH=%PREFIX%\Library\usr\bin;%PATH%" && column --help && fallocate --help && fdisk --help && findfs --help && flock --help && fsck.cramfs --help && fsck.minix --help && getopt --help # [win]
        - set "PATH=%PREFIX%\Library\usr\bin;%PATH%" && hardlink --help && hexdump --help && ipcmk --help && isosize --help && lastlog2 --help && logger --help && look --help && mcookie --help # [win]
        - set "PATH=%PREFIX%\Library\usr\bin;%PATH%" && mkfs --help && mkfs.bfs --help && mkfs.cramfs --help && mkfs.minix --help && mkswap --help && more --help && namei --help && pg --help # [win]
        - set "PATH=%PREFIX%\Library\usr\bin;%PATH%" && renice --help && rev --help && setpgid --help && setsid --help && sfdisk --help && swaplabel --help && taskset --help && ul --help # [win]
        - set "PATH=%PREFIX%\Library\usr\bin;%PATH%" && uuidd --help && uuidgen --help && uuidparse --help && whereis --help && wipefs --help # [win]
    about:
      home: https://www.kernel.org/pub/linux/utils/util-linux/
      summary: "Miscellaneous system utilities for Linux (repack of MSYS2-packages util-linux for MSYS)"
      license: GPL-2.0-only
      license_file:
        - {{ RECIPE_DIR }}/src_licenses/COPYING

  - name: msys2-libutil-linux
    skip: True          # [not (win)]
    script: repack.bat  # [win]
    build:
      number: {{ build_number }}
      missing_dso_whitelist:
        - $RPATH/KERNEL32.dll                                # [win]
        - $RPATH/msys-2.0.dll                                # [win]
        - $RPATH/msys-gcc_s-seh-1.dll                        # [win]
        - $RPATH/msys-intl-8.dll                             # [win]
        - $RPATH/msys-sqlite3-0.dll                          # [win]
    requirements:
      build:
      host:
      run:
        - msys2-msys-mutex * msys2
        - msys2-gcc-libs >=15.3.0                  # gcc-libs
        - msys2-libintl >=0.22.5                   # libintl
        - msys2-msys2-runtime >=3.6.9              # DSO msys-2.0.dll
        - msys2-libsqlite >=3.53.3                 # DSO msys-sqlite3-0.dll
      run_constrained:
        - m2-libutil-linux                         <0a0
        - m2-msys2-runtime                         <0a0
    test:
      commands:
        - test -f $PREFIX/Library/usr/bin/msys-blkid-1.dll                       # [unix]
        - IF NOT EXIST %PREFIX%\Library\usr\bin\msys-blkid-1.dll EXIT /B 1       # [win]
    about:
      home: https://www.kernel.org/pub/linux/utils/util-linux/
      summary: "Miscellaneous system utilities for Linux (repack of MSYS2-packages libutil-linux for MSYS)"
      license: GPL-2.0-only
      license_file:
        - {{ RECIPE_DIR }}/src_licenses/COPYING

  - name: msys2-libutil-linux-devel
    skip: True          # [not (win)]
    script: repack.bat  # [win]
    build:
      number: {{ build_number }}
    requirements:
      build:
      host:
      run:
        - msys2-msys-mutex * msys2
        - msys2-libutil-linux >=2.40.2             # libutil-linux
      run_constrained:
        - m2-libutil-linux-devel                   <0a0
        - m2-msys2-runtime                         <0a0
    test:
      commands:
        - test -f $PREFIX/Library/usr/include/blkid/blkid.h                      # [unix]
        - IF NOT EXIST %PREFIX%\Library\usr\include\blkid\blkid.h EXIT /B 1      # [win]
    about:
      home: https://www.kernel.org/pub/linux/utils/util-linux/
      summary: "Miscellaneous system utilities for Linux (repack of MSYS2-packages libutil-linux-devel for MSYS)"
      license: GPL-2.0-only
      license_file:
        - {{ RECIPE_DIR }}/src_licenses/COPYING

